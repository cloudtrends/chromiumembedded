Index: common.gypi
===================================================================
--- common.gypi	(revision 46337)
+++ common.gypi	(working copy)
@@ -15,6 +15,9 @@
 
     # Variables expected to be overriden on the GYP command line (-D) or by
     # ~/.gyp/include.gypi.
+    
+    # Directory for CEF2 source files. This will be set by cef2.gypi.
+    'cef2_directory%' : '',
 
     # Putting a variables dict inside another variables dict looks kind of
     # weird.  This is done so that "branding" and "buildtype" are defined as
@@ -1159,6 +1162,13 @@
           '_SECURE_ATL',
           '_HAS_TR1=0',
         ],
+        'conditions': [
+          ['cef2_directory!=""', {
+            'include_dirs': [
+              '<(DEPTH)/<(cef2_directory)',
+            ],
+          }],
+        ],
         'msvs_system_include_dirs': [
           '<(DEPTH)/third_party/platformsdk_win7/files/Include',
           '$(VSInstallDir)/VC/atlmfc/include',
Index: win/system.gyp
===================================================================
--- win/system.gyp	(revision 46337)
+++ win/system.gyp	(working copy)
@@ -22,6 +22,13 @@
           'action': ['', '<@(_inputs)'],
         },
       ],
+      'conditions': [
+        ['cef2_directory!=""', {
+          'dependencies': [
+            '../../<(cef2_directory)/cef2_browser.gyp:patcher',
+          ],
+        }],
+      ],
     },
   ],
 }
